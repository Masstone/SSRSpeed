name: Sync Fork

on:
  schedule:
  - cron: '* * * * *'

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
    - uses: TG908/fork-sync@v1.1.4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        owner: NyanChanMeow
        base: dev
        head: dev
